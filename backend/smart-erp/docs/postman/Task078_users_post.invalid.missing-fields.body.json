{
  "_description": "Task078 — thiếu toàn bộ field bắt buộc (body rỗng) → HTTP 400 + details. Tiền đề giống file valid: APP_SECURITY_MODE=jwt-api + Bearer accessToken còn hạn (xem Task078_users_post.valid.body.json § tránh 401).",
  "request": {
    "method": "POST",
    "path": "/api/v1/users",
    "url": "http://localhost:8080/api/v1/users"
  },
  "headers": {
    "Content-Type": "application/json",
    "Authorization": "Bearer <paste-accessToken-from-Task001-login-response.data.accessToken>"
  },
  "body": {}
}
