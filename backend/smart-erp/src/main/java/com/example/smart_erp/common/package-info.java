/**
 * Shared building blocks: API envelopes ({@code com.example.smart_erp.common.api}),
 * global exception handling, validation helpers, utilities.
 * No rules that belong exclusively to one business area.
 * <p>
 * Nghiệp vụ theo module: {@code auth.*} (xác thực), {@code users.*} (nhân viên), {@code config.*}, tương lai {@code inventory.*}, {@code sales.*}.
 */
package com.example.smart_erp.common;
