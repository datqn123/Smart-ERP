/**
 * Inventory: tồn kho, thay đổi tồn, vị trí. Task005 — {@code GET /api/v1/inventory} (list + summary);
 * Task009 — {@code GET /api/v1/inventory/summary} (KPI only);
 * Task006 — {@code GET /api/v1/inventory/{id}} (chi tiết + {@code relatedLines});
 * Task007 — {@code PATCH /api/v1/inventory/{id}} (meta + migration {@code unit_id}).
 */
package com.example.smart_erp.inventory;
