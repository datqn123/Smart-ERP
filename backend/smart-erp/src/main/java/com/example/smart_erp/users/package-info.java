/**
 * Quản lý người dùng / nhân viên (API CRUD theo task).
 * <p>
 * Cấu trúc: {@code users.controller}, {@code users.service}, {@code users.dto} (request),
 * {@code users.response} (payload API).
 */
package com.example.smart_erp.users;
