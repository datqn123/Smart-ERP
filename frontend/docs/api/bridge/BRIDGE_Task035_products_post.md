# BRIDGE — Task035 — `POST /api/v1/products`

> **Task:** Task035 | **Path:** `POST /api/v1/products` | **Mode:** wire-fe + verify multipart | **Date:** 27/04/2026 *(cập nhật theo SRS §14 / OQ-7)*

**Đã đọc** [`frontend/AGENTS/docs/FE_API_CONNECTION_GUIDE.md`](../../AGENTS/docs/FE_API_CONNECTION_GUIDE.md): Y

| Hạng mục | API doc (`API_Task035_products_post.md`) + SRS §8.3 / **§14.2** / **OQ-2(a)** / **C3** / **BR-9** | Backend | Frontend | Khớp | Hành động / ghi chú |
| :-- | :-- | :-- | :-- | :--: | :-- |
| Body camelCase | §8.3 JSON | [`ProductCreateRequest`](../../../../backend/smart-erp/src/main/java/com/example/smart_erp/catalog/dto/ProductCreateRequest.java) | [`buildProductCreateBody`](../../../mini-erp/src/features/product-management/api/productsApi.ts) + [`postProduct`](../../../mini-erp/src/features/product-management/api/productsApi.ts) | Y | `categoryId` 0 / thiếu → `null`; `weight` 0 → `null`; `priceEffectiveDate` rỗng → không gửi key. |
| **Multipart OQ-7** | Part `metadata` (JSON cùng schema §8.3) + nhiều part `file`; tối đa 10 file/SRS; `primaryImageIndex` (0-based) | `ProductsController` / `ProductService.createWithImageFiles` (SRS §14.7) | [`postProductCreateMultipart`](../../../mini-erp/src/features/product-management/api/productsApi.ts): `FormData` append `metadata` blob, lặp `file`, field `primaryImageIndex` | Y | Cần Cloudinary bật khi gửi file; một file lỗi → all-or-nothing §14.4. |
| **Sau 201 — URL staged** | Task039 JSON | — | [`createProductMutation`](../../../mini-erp/src/features/product-management/pages/ProductsPage.tsx): nếu chỉ JSON create hoặc sau multipart, lần lượt `postProductImageJson(created.id, …)` cho mỗi phần tử `staged.urlAdds` | Y | Ảnh file trong multipart đã persist; `urlAdds` là bổ sung URL sau tạo. |
| **UI tạo — ảnh chỉ khi Lưu** | SRS §14.1 mục 4 / **BR-10** (áp dụng form; tạo không có `productId` trước Lưu) | — | [`ProductImagePanel`](../../../mini-erp/src/features/product-management/components/ProductImagePanel.tsx) + `staged` / `onStagedChange` từ [`ProductsPage`](../../../mini-erp/src/features/product-management/pages/ProductsPage.tsx) → `StagedProductImages` trong [`productsApi.ts`](../../../mini-erp/src/features/product-management/api/productsApi.ts) | Y | Không gọi `POST …/images` cho file cho tới khi submit form tạo. |
| **201** + invalidate | List sau tạo | `create` + `createMultipart` | `createProductMutation` + [`ProductForm`](../../../mini-erp/src/features/product-management/components/ProductForm.tsx) `onSubmit` | Y | — |
| **400** `details` → field | Envelope | Bean validation / `BusinessException` | [`ProductForm`](../../../mini-erp/src/features/product-management/components/ProductForm.tsx) `setError` theo key trong `details` (whitelist field); **INVALID_CATEGORY** → có thể kèm `details.categoryId` | **Partial** | `createProductMutation.onError` → [`toastProductMutationEnvelope`](../../../mini-erp/src/features/product-management/pages/ProductsPage.tsx). |
| **409** SKU / **404** category / **400** INVALID_CATEGORY | Toast | Như BE | Cùng `toastProductMutationEnvelope`; 400 có `details` (field) → form xử lý, tránh toast trùng theo comment Task032-style trên `ProductsPage` | Y | — |
| UI tạo mới | `ProductForm` dialog | — | **Đơn vị cơ sở** hiện hardcode `"Cái"` trong submit create ([`ProductsPage`](../../../mini-erp/src/features/product-management/pages/ProductsPage.tsx)); giá / ngày hiệu lực như form | Y | Có thể mở rộng field `baseUnitName` sau. |

**Kết luận:** JSON-only và multipart **một bước** (metadata + files) đã nối; ảnh trên form tạo chỉ đẩy lên server khi **Lưu** (`createProductMutation`). Đồng bộ doc chi tiết: `Mode=fix-doc` trên `API_Task035_products_post.md` + `samples/Task035/` theo SRS §15.2.
