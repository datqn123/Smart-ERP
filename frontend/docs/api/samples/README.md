# Mẫu JSON (request body / response envelope)

Mỗi thư mục `TaskXXX/` tương ứng một task; tên file: `<slug>.request.json`, `<slug>.response.<httpStatus>.json`.

**Index bảng** (Path, link spec, link Postman): [`../endpoints/`](../endpoints/).
