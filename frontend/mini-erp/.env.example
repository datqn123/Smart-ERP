# Copy to .env.local — Spring Boot default local (FE dev: npm run dev → http://localhost:3000)
# Backend must allow CORS for that origin (see smart-erp app.cors.allowed-origin-patterns).
VITE_API_BASE_URL=http://localhost:8080
